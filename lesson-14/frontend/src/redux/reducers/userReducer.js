const initialState = {
  user: {},
  loader: false,
  error: "",
};

export const userReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
